<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Siyag Stone — Granite Exporters</title>
  <meta name="description" content="Siyag Stone — Premium black granite exporters. Quality-controlled blocks, custom cutting, strong international packing, and reliable container shipments.">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700;800&display=swap" rel="stylesheet">
  <style>
    :root{
      --accent:#0b7285; /* teal */
      --dark:#0f1724;
      --muted:#6b7280;
      --card:#ffffff;
      --bg:#f7fbfc;
      --max:1200px;
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0;font-family:Inter,system-ui,-apple-system,"Segoe UI",Roboto,Helvetica,Arial,"Noto Sans",sans-serif;background:var(--bg);color:var(--dark);-webkit-font-smoothing:antialiased;
    }
    .container{max-width:var(--max);margin:0 auto;padding:28px}
    header{background:linear-gradient(90deg,rgba(11,114,133,.06),transparent);padding:20px 0;border-bottom:1px solid rgba(15,23,36,.04)}
    .nav{display:flex;align-items:center;justify-content:space-between;gap:16px}
    .brand{display:flex;align-items:center;gap:12px}
    .logo{width:56px;height:56px;border-radius:8px;background:linear-gradient(180deg,var(--accent),#0b6b80);display:flex;align-items:center;justify-content:center;color:#fff;font-weight:800;font-size:20px;box-shadow:0 6px 18px rgba(11,114,133,.12)}
    nav a{margin-left:18px;color:var(--dark);text-decoration:none;font-weight:600}
    .hero{display:grid;grid-template-columns:1fr 420px;gap:28px;align-items:center;padding:48px 0}
    .eyebrow{color:var(--accent);font-weight:700;font-size:13px}
    h1{margin:8px 0 12px;font-size:36px;line-height:1.02}
    p.lead{color:var(--muted);margin:0 0 18px}
    .cta-row{display:flex;gap:12px}
    .btn{display:inline-flex;align-items:center;gap:10px;padding:12px 16px;border-radius:10px;border:0;cursor:pointer;font-weight:700}
    .btn-primary{background:var(--accent);color:#fff;box-shadow:0 8px 20px rgba(11,114,133,.12)}
    .btn-ghost{background:transparent;border:1px solid rgba(15,23,36,.06);color:var(--dark)}

    .hero-right{background:linear-gradient(180deg,#fff,#fbfeff);padding:20px;border-radius:12px;box-shadow:0 8px 30px rgba(11,114,133,.06)}
    .specs{display:flex;flex-direction:column;gap:10px}
    .spec{display:flex;justify-content:space-between;font-weight:600}

    section.features{margin-top:18px}
    .grid{display:grid;grid-template-columns:repeat(3,1fr);gap:18px;margin-top:18px}
    .card{background:var(--card);padding:18px;border-radius:10px;box-shadow:0 10px 24px rgba(15,23,36,.03)}
    .card h3{margin:0 0 8px}
    .gallery{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin-top:16px}
    .gallery img{width:100%;height:160px;object-fit:cover;border-radius:8px}

    .about{display:flex;gap:24px;margin-top:28px}
    .about .text{flex:1}
    .about .facts{width:320px;background:linear-gradient(180deg,#fff,#fcfeff);padding:18px;border-radius:10px;box-shadow:0 8px 24px rgba(15,23,36,.03)}

    .contact-section{margin-top:40px;padding:28px;background:linear-gradient(180deg,#ffffff,#f8fbfb);border-radius:12px;box-shadow:0 10px 30px rgba(15,23,36,.03)}
    .contact-grid{display:grid;grid-template-columns:1fr 420px;gap:18px}
    form{display:flex;flex-direction:column;gap:12px}
    input,textarea,select{padding:12px;border-radius:8px;border:1px solid rgba(15,23,36,.06);font-size:15px}
    textarea{min-height:120px;resize:vertical}
    .contact-details{display:flex;flex-direction:column;gap:10px}
    .detail{display:flex;gap:10px;align-items:center}
    .badge{background:rgba(11,114,133,.08);color:var(--accent);padding:8px 10px;border-radius:8px;font-weight:700}

    footer{margin-top:40px;padding:28px 0;border-top:1px solid rgba(15,23,36,.04);color:var(--muted);display:flex;justify-content:space-between;align-items:center}

    @media(max-width:960px){
      .hero{grid-template-columns:1fr;}
      .contact-grid{grid-template-columns:1fr}
      .grid{grid-template-columns:repeat(2,1fr)}
      .gallery{grid-template-columns:repeat(2,1fr)}
      .logo{width:48px;height:48px}
    }
    @media(max-width:520px){
      h1{font-size:26px}
      .grid{grid-template-columns:1fr}
      .gallery img{height:120px}
    }
  </style>
</head>
<body>
  <header>
    <div class="container nav">
      <div class="brand">
        <div class="logo">SS</div>
        <div>
          <div style="font-weight:800">Siyag Stone</div>
          <div style="font-size:12px;color:var(--muted);margin-top:2px">Premium Granite Exporters</div>
        </div>
      </div>
      <nav>
        <a href="#about">About</a>
        <a href="#products">Products</a>
        <a href="#gallery">Gallery</a>
        <a href="#contact">Contact</a>
      </nav>
    </div>
  </header>

  <main class="container">
    <section class="hero">
      <div>
        <div class="eyebrow">EXPORT • WHOLESALE • CUSTOM CUTTING</div>
        <h1>Siyag Stone — Reliable Black Granite for International Markets</h1>
        <p class="lead">We supply precision-cut black granite blocks, slabs and finished products with export-grade packing and full documentation support. Quality inspected, custom sizes, and secure container loading.</p>
        <div class="cta-row">
          <a class="btn btn-primary" href="#contact">Request Quote</a>
          <a class="btn btn-ghost" href="#products">View Products</a>
        </div>

        <section class="features">
          <div class="grid">
            <div class="card">
              <h3>Export Packaging</h3>
              <p style="color:var(--muted);margin:0">Durable wooden crates, steel bands, anti-shock packing — container-ready.</p>
            </div>
            <div class="card">
              <h3>Quality Control</h3>
              <p style="color:var(--muted);margin:0">Pre-shipment inspection, certified measurements, consistent finishes.</p>
            </div>
            <div class="card">
              <h3>Custom Cutting</h3>
              <p style="color:var(--muted);margin:0">Tailored slab sizes, polished or honed finishes, strict tolerance control.</p>
            </div>
          </div>
        </section>
      </div>

      <aside class="hero-right">
        <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:12px">
          <div style="font-weight:700">Get a quick quote</div>
          <div style="font-size:13px;color:var(--muted)">Typical response within 24 hrs</div>
        </div>
        <div class="specs">
          <div class="spec"><span>Origin</span><span>India</span></div>
          <div class="spec"><span>Material</span><span>Black Granite</span></div>
          <div class="spec"><span>Available</span><span>Blocks • Slabs • Tiles</span></div>
          <div class="spec"><span>Packing</span><span>Export-grade crates</span></div>
        </div>
        <div style="margin-top:12px;font-size:13px;color:var(--muted)">Please include project details (dimensions, finish, quantity) when requesting a quote.</div>
      </aside>
    </section>

    <section id="about" class="about" aria-labelledby="about-heading">
      <div class="text">
        <h2 id="about-heading">About Siyag Stone</h2>
        <p style="color:var(--muted);margin-top:6px">Siyag Stone is a family-owned granite supplier specialising in export quality black granite. With in-house cutting, polishing and packing facilities, we serve international builders, architects and distributors. We export full containers and LCL shipments with complete export documentation, inspection reports and packing l
